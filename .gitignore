# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.benchmarks/

# Virtual environments
venv/
.venv/
.venv-*/
env/

# Tag store / generated data
*.db3
*.sqlite
*.sqlite3
*.db3-wal
*.db3-shm

# Logs and run artifacts
*.log
*.stdout

# Large downloaded dependencies (see SETUP.md)
*.dll
*.pt
libmpv-*.dll
hidapi*.zip
hidapi-tmp/
yolov8n-pose.pt

# Local config and state
settings.json
*_state.json
saved_searches.json

# Generated demo clips (run `python make_samples.py` to recreate)
samples/

# Sanitization audit report — lists the scrubbed content terms by design,
# so it must never be committed to the public repo. Regenerate locally.
_sanitize_audit_report.md

# Sanitization audit LOCAL extension term-list — holds the user's
# testbed-library subfolder names (performers, categories) so the audit
# can catch content-specific leaks the generic in-source denylist can't.
# These names ARE the sensitive content; never commit. Regenerate locally
# with the populate helper (see _sanitize_audit.py header).
_sanitize_audit.extra-terms.txt

# Claude session context — personal notes, iteration log, hard-won lessons.
# Private; never committed to the public repo.
CLAUDE.md
_session_notes/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
